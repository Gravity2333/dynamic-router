import { RouteProps } from '../typings';
export default function Route({ meta, path, component, render, children, sensitive, exact, computedMatch, }: RouteProps): import("react/jsx-runtime").JSX.Element | null;
